# AnchorWorks Operational Truth Lock

- Created: 2026-05-24
- Authority: code, tests, receipts, and this document outrank older planning docs when they conflict.
- Scope: AnchorWorks only. SecureCore and TrueVision Generation remain separate systems.

## Core Law

```text
AnchorWorks is the face and language/count brain.
Lexicon recognizes.
Counts weigh.
Clouds shape.
Inference admits.
Renderer walks.
Receipts prove.
```

## Current Live Abilities

```text
genome-native symbol authority
lexicon-first recognition
symbol relation count building
C++ binary AWSC count spine
ClearSpeak walked-count output tests
query punctuation as direction, not content evidence
formula lane proof fixtures
phrase authority docs/contracts
TrueVision language-state branch
Document-as-movie state recording
visual glyph lexicon and glyph-state intake
Occular Cloud 6x4-4-6x4 count windows
Occular tensor shard store/index/query
OpenVINO/GPU-visible accelerated Occular lane with CPU parity
Four-anchor Unit4 sibling counts
Grounded evidence packet/render mode
repo code lexicon mirror builder for Python/Rust identifiers
```

## Parked Or Separate

```text
UI and Help are parked.
Help later reads the same action/control map as UI.
TrueAudio remains in TrueVision Generation.
SecureCore security logging remains in SecureCore.
Generated media/state artifacts remain runtime data, not source code.
```

## Current Staged Work

```text
edge_state_intake: planned owned-device compact state intake lane.
proper memory scaffolding: next backend lock.
Unit4 intake adapter: next after memory scaffold if still needed.
```

## Code Lexicon Mirror Truth

The code lexicon mirror exists as review/runtime state:

```text
State/code_lexicon_mirror/code_lexicon_mirror.json
```

It is not Canonical, not Structural, not live lexicon authority, and not count memory.

```text
mirror only
promotion required
no live lexicon mutation
no counts mutation
no lifetime mutation
```

## Count Shape Truth

Base language count shape:

```text
6-1-6
```

Current visual/Occular count shape:

```text
6x4-4-6x4
```

Current Unit4 sibling count shape:

```text
6-1-6 over unit4
unit4 = fixed ordered groups of 4 anchors from the start of the stream
```

Unit4 is an addition beside solo-anchor counts. It does not replace, rewrite, or promote into normal lifetime counts.

## Future Ability Area

Everything below remains future until code, tests, and receipts prove it:

```text
UI/request-time cards
Help chat shard
cross-system harness
edge device sync
encrypted edge vault
automatic map conversion jobs
global promotion of code mirror identifiers
```

## Tiny Lock

```text
Do not tamper with proven state.
Adapt through contracts.
Write receipts.
Test the shape.
Then expose it.
```
