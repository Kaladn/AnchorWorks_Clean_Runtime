# Architecture Path Trace

Current V1 runtime trace.

## Source Intake

```text
source file
-> text/visual preparation
-> anchor extraction
-> lexicon recognition
-> symbolic source-local material
-> native count build when count memory is requested
```

Status: active, needs more native-only tightening for full rebuilds.

## Lexicon Lookup

```text
input surface
-> anchor normalization
-> Canonical / Structural genome lexicon
-> genome symbol
```

Status: active.

Live identity: genome.

## Count Memory

```text
genome symbol
-> genome-native AWSC cell
-> C++ verified count memory
```

Status: active for current rebuilt count root.

Rule:

```text
No runtime identity aliasing.
No Python bulk count-cell processing.
```

## ClearSpeak Renderer Path

```text
query
-> lexicon-first recognition
-> genome symbol count lookup
-> active Q/R/A/F cloud walk
-> top-K candidate pressure
-> lookahead health
-> inference admission
-> answer plan
-> human rendered text
```

Status: active, still being tuned.

## Inference Kernel

```text
candidate answer path
-> frame gate
-> support gate
-> domain/frame rejection
-> answer plan
```

Status: active for counts mode.

## Documents Path

```text
query
-> lexicon recognition
-> flat document evidence search
-> local 6-1-6 source cloud
-> grounded answer frame
```

Status: active but secondary. Do not let documents override counts-mode tuning.

## System-Doc Lane

Status: required tightening.

Rule:

```text
system-self docs answer system-self questions only
education/general answers use education/general lanes
```

## Settings

Status: partially active, truth cleanup required.

Rule:

```text
No visible control unless runtime honors it or it is clearly diagnostic-only.
```

## Excluded Runtime Authority

```text
Spare_Slots
runtime identity aliases
Python bulk count-cell migration
raw top-K speech
```

## Verification Path

```powershell
python -m unittest discover -s "D:\AnchorWorks_Clean_Runtime\test\test data\tests" -q
```

Chart law:

```text
A box may appear as active only if this trace or code proves it is connected.
```
