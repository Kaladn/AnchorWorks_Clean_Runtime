# Architecture Chart Changes

Date: 2026-05-14

Use this as the change list for the next architecture chart. The goal is to remove pretty-but-unproven boxes and replace them with traced runtime truth.

## Must Add

```text
Genome/Legacy Count Bridge
Local Meta-Count Overlays
AWSG Proof Graph
External Map Root: D:\AnchorMaps
Test/Test Data Root
AWSC Tiny-Cell Fanout Warning
V2 Boundary Brain as external staged shell
```

## Must Rename

```text
Observed Maps
  -> Observed JSON Debug/Audit Maps

Symbolic Maps
  -> AWSM Symbolic Relation Maps

Visual Data
  -> AWSV Visual Reference Sidecars

Renderer
  -> ClearSpeak Symbol Walk + Q/R/A/F Active Cloud + Top-K Lookahead

Phrases
  -> Phrase Authority, staged empty
```

## Must Downgrade

```text
AWSG
  from active production graph
  to staged/proof graph unless source_graphs contains production graphs

Invoked UI
  from runtime feature
  to planned V2 recipe surface

V2
  from runtime system
  to external contract/control-plane destination

Phrase Lexicon
  from active populated authority
  to active code path with zero live rows
```

## Must Mark Legacy

```text
Spare_Slots
legacy spare pool / excluded authority

Legacy JSON lifetime monolith
not hot path

tone_signature/font_symbol/frequency identity fields
legacy residue, not live minimal lexicon authority
```

## Must Show As Active

```text
Canonical word -> genome symbol
Symbol genome pool and checkpoint
AWSM hot path for source-local symbol counts when AWSM exists
AWSS stream writer
C++ AWSC merge-stream/verify/inspect/score wrapper
ClearSpeak AWSC count reader
Q/R/A/F active cloud scoring
Top-K lookahead future-shape check
Test harness at test\test data
```

## Must Show As Known Gap

```text
Current AWSC cells are not genome-native.
ClearSpeak uses compatibility mapping from old symbols to anchors.
Final count identity requires rebuild/remap/shard plan.
```

## Do Not Show As Active

```text
GPU sparse matrix scoring
Packed AWSC shard/index store
Production AWSG graph routing
Invoked UI chat cards
V2 runtime execution
Phrase UI toggle
Full user/workspace identity learning
```

## New Chart Law

```text
A box may appear as active only if a traced path proves it is connected.
If a box is useful but not connected, label it staged or planned.
If a box is old authority, label it legacy/excluded.
```

