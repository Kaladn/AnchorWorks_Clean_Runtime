# Architecture Chart Changes

Current chart update rules:

```text
Remove scaffold identity paths from the active chart.
Show genome-native count memory as the only live count identity path.
Show native C++ as the only bulk count builder/verifier.
Show Python as orchestration only.
Show Spare_Slots as excluded from runtime authority.
```

## Add Or Keep

```text
Canonical genome lexicon -> genome symbol lookup
Genome symbol lookup -> genome-native AWSC cells
C++ merge-stream / merge-symbol-stream / verify / inspect / score
ClearSpeak -> active cloud walk -> inference admission -> answer surface
Formula lane before evidence lane for formula-shaped questions
```

## Remove From Active Chart

```text
runtime identity aliases
Python count-cell migration
raw top-K speech
fake settings controls
unwired UI surfaces
```

## Chart Law

```text
A box is active only if a traced runtime path proves it is connected.
Scaffolds build the house. They do not live in the walls.
```
