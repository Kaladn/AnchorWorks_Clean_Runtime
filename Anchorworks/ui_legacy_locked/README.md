# UI Legacy Locked

Existing UI shells are preserved for reference, but the active server path must not route here.

Current active UI root:

```text
Anchorworks/UI2/
```

Rules:

- do not delete legacy UI work
- do not serve legacy UI from FastAPI
- do not import legacy scripts from UI2
- copy behavior forward only after confirming a backend route or runtime setting honors it
